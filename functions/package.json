{
  "name": "twin-ar-functions",
  "version": "0.1.0",
  "private": true,
  "description": "TWin AR Cloud Functions - Phase 8.13 real Stripe integration with trusted server-side stock reservation. Separate Node/TypeScript toolchain; NOT part of the Flutter app or its build.",
  "type": "commonjs",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "lint": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts",
    "test:emulator": "firebase emulators:exec --config ../firebase.json --only firestore,storage --project demo-twin-ar-fns \"vitest run --config vitest.emulator.config.ts\"",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "stripe": "^17.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
