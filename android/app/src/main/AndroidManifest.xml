<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Phase 9.2 R5 — Tier-2 Marker-AR uses the rear camera for pure-vision
         (OpenCV ArUco) tracking. No audio, nothing recorded/uploaded.
         camera is not required so non-camera devices still install (they get
         the Tier-3 3D preview later). Phase 9.2 R6 — Tier-1 markerless ARCore
         shares this same CAMERA permission (requested once, Dart-side, by
         `RoomArCapabilityService`/the prep screen — no separate permission
         flow); `android.hardware.camera.ar` is also declared `required="false"`
         so a device without full ARCore hardware support still installs and
         simply never gets offered Tier 1. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera.any" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.ar" android:required="false"/>
    <uses-feature android:glEsVersion="0x00030000" android:required="false"/>

    <application
        android:label="TWin AR"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Phase 9.2 R6 — "optional" (not "required"): the app must remain
             installable on devices Google Play doesn't list as ARCore-capable,
             or that don't have Google Play Services for AR installed at all —
             those devices simply never get Tier 1 (`RoomArCapabilitiesPlugin`
             reports `arCoreAvailable: false` for them; routing falls through
             to Tier 2/3, exactly the existing R8 behaviour). -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="optional" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
